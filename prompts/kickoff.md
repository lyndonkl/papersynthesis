Use the Agent tool to spawn the `papersynthesis-orchestrator` subagent. Pass it the context I've shared alongside this message — anything I've said (or will say in my next message) about the topic, time window, or how I want the digest to read. The orchestrator handles everything else.
