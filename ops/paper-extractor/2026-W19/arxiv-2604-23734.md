---
paper_id: arxiv:2604.23734
title: "Prism-Reranker: Beyond Relevance Scoring -- Jointly Producing Contributions and Evidence for Agentic Retrieval"
authors: ["Dun Zhang"]
date: 2026-04-26
source: arxiv
url: https://arxiv.org/abs/2604.23734
pdf_url: https://arxiv.org/pdf/2604.23734v1
primary_category: cs.IR
categories: ['cs.IR']
matched_keywords: ['retrieval-augmented generation', 'rag', 'reranker']
prefilter_score: 13.12
extracted_on: 2026-05-09
passes_completed: [1, 2]
---

## Pass 1 — Inspectional Reading

### Source abstract
Modern retrieval pipelines increasingly serve downstream consumers like retrieval-augmented generation (RAG) and autonomous agents that need more than a scalar relevance score. A reranker that only tells the caller "how relevant" forces the agent to dump entire documents into the language-model context, wasting tokens on tangential passages and boilerplate. We introduce Prism-Reranker, a family of reranker models built on Qwen3.5 at four sizes (0.8B, 2B, 4B, 9B) that goes beyond scalar scoring. In addition to the standard yes/no relevance judgement, whenever the verdict is yes the model emits (i) a contribution statement summarizing how the document helps the query, and (ii) an evidence passage: a self-contained rewrite that preserves every query-relevant signal while discarding noise. Prism-Reranker is trained with a hybrid objective combining point-wise distillation from a strong commercial reranker API with supervised fine-tuning on contribution and evidence targets. We curate training data from KaLM-Embedding's open-source aggregation, augmented with real web documents retrieved via commercial search APIs for open-domain queries and LLM-synthesized variants, and rewrite a portion of queries into keyword-style reformulations to adapt the model to agent-issued traffic. To reconcile inconsistent labels across open corpora and obtain crisp binary supervision, we relabel data with an LLM-as-Judge ensemble aggregating votes from five frontier LLMs. On a QA subset of BEIR and on an LLM-judged evaluation of contribution and evidence quality, Prism-Reranker attains solid results across all four sizes. We further show that the same recipe extends existing LLM-based rerankers, augmenting Qwen3-Reranker-4B with contribution and evidence capabilities while improving its average BEIR-QA NDCG@10 by +1.54 over the base model. Model weights, training recipe, and evaluation suite are released.

### Five Cs

- **Category**: methods paper (with empirical evaluation + dataset/recipe release)
- **Context**: Sits within agentic-RAG pipelines where rerankers feed an LLM that wastes tokens on full documents; extends Qwen3-Reranker by adding 'contribution' + 'evidence-passage' outputs beyond scalar relevance.
- **Correctness (first-glance)**: no first-glance concerns. Reports BEIR-QA NDCG@10 lift of +1.54 over a strong baseline (Qwen3-Reranker-4B). Trained with LLM-as-Judge ensemble of 5 frontier LLMs - that's a non-trivial label-noise reduction step.
- **Contributions**: New reranker family (0.8/2/4/9B) that emits a contribution statement and an evidence-passage rewrite alongside the relevance verdict. Released model weights + training recipe + eval suite. Reports +1.54 NDCG@10 average on BEIR-QA when applied as an extension to Qwen3-Reranker-4B.
- **Clarity**: clear. Argument is sharp: rerankers should output what the agent needs (evidence, not just score), not what IR papers historically scored.

### One-line
Trains rerankers (0.8B-9B) that emit a contribution statement and an evidence-passage rewrite in addition to a relevance verdict, so agents stop dumping whole documents into context.


## Pass 2 — Content Grasp (degraded mode)

> **Note**: full_text_available=false. Pass 2 was conducted from the abstract
> only (arXiv preprint, accessed 2026-05-09). The orchestrator's pragmatic
> trade-off for a 25-paper KEEP set: Pass 2 from rich abstracts retains most
> of the per-paper signal needed for downstream Pass A (within-paper layered
> summary) at much lower compute cost than 25 full-PDF reads. Confidence is
> reduced; figure-level analysis and reference triage are unavailable.
> Pass 3 (deep read) remains available on operator request.

### What survives at Pass 2 in degraded mode
- Main-argument identification (from abstract's structure).
- Big-question framing (from abstract's positioning paragraph).
- Hypothesis / design-claim list (often explicit in technique-paper abstracts).
- Unfamiliar-terms inventory (terms-of-art the synthesizer must gloss).

### What is unavailable in degraded mode
- Figure-by-figure analysis (would need full text + figures).
- Reference-triage (load-bearing vs background) — not extracted from abstract.
- Mid-paper confusions / unresolved-points list — not extractable from abstract.

### Confidence tag for downstream synthesis
The Pass A summary in `ops/paper-synthesizer/2026-W19/per-paper/` should be
read as 'abstract-only synthesis with hedging preserved.' If the operator
wants higher confidence on a specific paper, escalate via a DEEP_READ
intent.
